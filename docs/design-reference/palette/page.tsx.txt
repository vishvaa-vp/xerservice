'use client';

import React, { useState, useMemo, useEffect } from 'react';
import Link from 'next/link';
import { useApp } from '@/context/AppContext';
import {
    Sun,
    Moon,
    Copy,
    Check,
    Palette,
    Sparkles,
    Search,
    Sliders,
    Layers,
    Code,
    Zap,
    RefreshCw,
    ShieldCheck,
    FileText,
    CheckCircle2,
    ArrowUpRight,
    Eye
} from 'lucide-react';

// ─── Color Helper Utilities ───

function hexToRgb(hex: string): { r: number; g: number; b: number } {
    let clean = hex.replace('#', '');
    if (clean.length === 3) {
        clean = clean.split('').map(c => c + c).join('');
    }
    const num = parseInt(clean, 16);
    return {
        r: (num >> 16) & 255,
        g: (num >> 8) & 255,
        b: num & 255,
    };
}

function rgbToHex(r: number, g: number, b: number): string {
    const clamp = (v: number) => Math.max(0, Math.min(255, Math.round(v)));
    return (
        '#' +
        [clamp(r), clamp(g), clamp(b)]
            .map(x => x.toString(16).padStart(2, '0'))
            .join('')
    );
}

function hexToHsl(hex: string): { h: number; s: number; l: number } {
    const { r: r255, g: g255, b: b255 } = hexToRgb(hex);
    const r = r255 / 255;
    const g = g255 / 255;
    const b = b255 / 255;

    const max = Math.max(r, g, b);
    const min = Math.min(r, g, b);
    let h = 0;
    let s = 0;
    const l = (max + min) / 2;

    if (max !== min) {
        const d = max - min;
        s = l > 0.5 ? d / (2 - max - min) : d / (max + min);
        switch (max) {
            case r:
                h = (g - b) / d + (g < b ? 6 : 0);
                break;
            case g:
                h = (b - r) / d + 2;
                break;
            case b:
                h = (r - g) / d + 4;
                break;
        }
        h /= 6;
    }

    return {
        h: Math.round(h * 360),
        s: Math.round(s * 100),
        l: Math.round(l * 100),
    };
}

function getLuminance(hex: string): number {
    const { r, g, b } = hexToRgb(hex);
    const a = [r, g, b].map(v => {
        const normalized = v / 255;
        return normalized <= 0.03928
            ? normalized / 12.92
            : Math.pow((normalized + 0.055) / 1.055, 2.4);
    });
    return a[0] * 0.2126 + a[1] * 0.7152 + a[2] * 0.0722;
}

function getContrastRatio(hex1: string, hex2: string): number {
    const lum1 = getLuminance(hex1);
    const lum2 = getLuminance(hex2);
    const brightest = Math.max(lum1, lum2);
    const darkest = Math.min(lum1, lum2);
    return Number(((brightest + 0.05) / (darkest + 0.05)).toFixed(2));
}

function getHoverVariant(hex: string, isDark: boolean): string {
    const { h, s, l } = hexToHsl(hex);
    // Darker in light mode, slightly lighter in dark mode for better visibility
    const newL = isDark ? Math.min(95, l + 8) : Math.max(15, l - 8);
    const c = (1 - Math.abs(2 * (newL / 100) - 1)) * (s / 100);
    const x = c * (1 - Math.abs(((h / 60) % 2) - 1));
    const m = newL / 100 - c / 2;
    let r = 0, g = 0, b = 0;
    if (h < 60) { r = c; g = x; }
    else if (h < 120) { r = x; g = c; }
    else if (h < 180) { g = c; b = x; }
    else if (h < 240) { g = x; b = c; }
    else if (h < 300) { r = x; b = c; }
    else { r = c; b = x; }
    return rgbToHex((r + m) * 255, (g + m) * 255, (b + m) * 255);
}

// ─── Curated Color Palettes ───

interface SwatchItem {
    name: string;
    hex: string;
    shade: number;
    recommendedFor?: 'light' | 'dark' | 'both';
    highlight?: boolean;
}

interface ColorFamily {
    id: string;
    name: string;
    tagline: string;
    description: string;
    badge: string;
    recommendedLight: string;
    recommendedDark: string;
    swatches: SwatchItem[];
}

const COLOR_FAMILIES: ColorFamily[] = [
    {
        id: 'modern-tech',
        name: 'Modern Tech Cyan (Tailwind / SaaS Standard)',
        tagline: 'Crisp, authoritative, and balanced. The modern developer & SaaS benchmark.',
        description:
            'A carefully balanced cyan spectrum with rich saturation and excellent readability. Ideal for modern digital products, developer tools, and clean SaaS dashboards.',
        badge: 'Recommended for XerService',
        recommendedLight: '#0891b2', // 600 for contrast
        recommendedDark: '#06b6d4',  // 500 for luminescence
        swatches: [
            { name: 'Cyan 50', hex: '#ecfeff', shade: 50 },
            { name: 'Cyan 100', hex: '#cffafe', shade: 100 },
            { name: 'Cyan 200', hex: '#a5f3fc', shade: 200 },
            { name: 'Cyan 300', hex: '#67e8f9', shade: 300 },
            { name: 'Cyan 400', hex: '#22d3ee', shade: 400, recommendedFor: 'dark' },
            { name: 'Cyan 500', hex: '#06b6d4', shade: 500, highlight: true, recommendedFor: 'both' },
            { name: 'Cyan 600', hex: '#0891b2', shade: 600, highlight: true, recommendedFor: 'light' },
            { name: 'Cyan 700', hex: '#0e7490', shade: 700, recommendedFor: 'light' },
            { name: 'Cyan 800', hex: '#155e75', shade: 800 },
            { name: 'Cyan 900', hex: '#164e63', shade: 900 },
            { name: 'Cyan 950', hex: '#083344', shade: 950 },
        ],
    },
    {
        id: 'electric-cyber',
        name: 'Electric Cyber Cyan (Vivid / Neon / High Glow)',
        tagline: 'High-octane, luminous, and energetic. Unmatched vibrance in dark mode.',
        description:
            'Hyper-saturated, punchy cyans inspired by cyberpunk interfaces and neo-fintech. Solves the washed-out feeling completely and produces brilliant glowing accents.',
        badge: 'Top Glow & Energy',
        recommendedLight: '#0096c7',
        recommendedDark: '#00e5ff',
        swatches: [
            { name: 'Electric 50', hex: '#f0fdff', shade: 50 },
            { name: 'Electric 100', hex: '#c9f7ff', shade: 100 },
            { name: 'Electric 200', hex: '#87eeff', shade: 200 },
            { name: 'Electric 300', hex: '#3de4ff', shade: 300 },
            { name: 'Electric 400', hex: '#00d5f2', shade: 400, recommendedFor: 'dark' },
            { name: 'Electric 500', hex: '#00e5ff', shade: 500, highlight: true, recommendedFor: 'dark' },
            { name: 'Electric 600', hex: '#00d2d3', shade: 600, highlight: true, recommendedFor: 'both' },
            { name: 'Electric 700', hex: '#00b4d8', shade: 700, recommendedFor: 'light' },
            { name: 'Electric 800', hex: '#0096c7', shade: 800, recommendedFor: 'light' },
            { name: 'Electric 900', hex: '#0077b6', shade: 900 },
            { name: 'Electric 950', hex: '#023e8a', shade: 950 },
        ],
    },
    {
        id: 'sky-azure',
        name: 'Sky Azure Cyan (Print & Xerox Modern Blue-Cyan)',
        tagline: 'Approachable, friendly, and classic. The authentic color of printing cyan.',
        description:
            'CMYK Cyan sits naturally between sky blue and pure cyan. This family feels deeply connected to document printing, precision imaging, and customer trust.',
        badge: 'Perfect Print Alignment',
        recommendedLight: '#0284c7',
        recommendedDark: '#38bdf8',
        swatches: [
            { name: 'Sky 50', hex: '#f0f9ff', shade: 50 },
            { name: 'Sky 100', hex: '#e0f2fe', shade: 100 },
            { name: 'Sky 200', hex: '#bae6fd', shade: 200 },
            { name: 'Sky 300', hex: '#7dd3fc', shade: 300 },
            { name: 'Sky 400', hex: '#38bdf8', shade: 400, highlight: true, recommendedFor: 'dark' },
            { name: 'Sky 500', hex: '#0ea5e9', shade: 500, highlight: true, recommendedFor: 'both' },
            { name: 'Sky 600', hex: '#0284c7', shade: 600, highlight: true, recommendedFor: 'light' },
            { name: 'Sky 700', hex: '#0369a1', shade: 700, recommendedFor: 'light' },
            { name: 'Sky 800', hex: '#075985', shade: 800 },
            { name: 'Sky 900', hex: '#0c4a6e', shade: 900 },
            { name: 'Sky 950', hex: '#082f49', shade: 950 },
        ],
    },
    {
        id: 'deep-oceanic',
        name: 'Deep Oceanic & React Teal (High Contrast / Trustworthy)',
        tagline: 'Premium, calm, and readable. Highest WCAG accessibility score on white.',
        description:
            'Inspired by deep ocean water and the iconic React developer aesthetic (#087ea4). Exceptional legibility for text badges, borders, and buttons on clean white backgrounds.',
        badge: 'Best WCAG Accessibility',
        recommendedLight: '#0e7490',
        recommendedDark: '#2dd4bf',
        swatches: [
            { name: 'Teal 50', hex: '#f0fdfa', shade: 50 },
            { name: 'Teal 100', hex: '#ccfbf1', shade: 100 },
            { name: 'Teal 200', hex: '#99f6e4', shade: 200 },
            { name: 'Teal 300', hex: '#5eead4', shade: 300 },
            { name: 'Teal 400', hex: '#2dd4bf', shade: 400, recommendedFor: 'dark' },
            { name: 'Teal 500', hex: '#14b8a6', shade: 500, highlight: true, recommendedFor: 'both' },
            { name: 'Teal 600', hex: '#0d9488', shade: 600, highlight: true, recommendedFor: 'light' },
            { name: 'Teal 700', hex: '#0f766e', shade: 700, recommendedFor: 'light' },
            { name: 'Teal 800', hex: '#115e59', shade: 800 },
            { name: 'React Blue-Teal', hex: '#087ea4', shade: 650, highlight: true, recommendedFor: 'light' },
            { name: 'Teal 950', hex: '#042f2e', shade: 950 },
        ],
    },
    {
        id: 'aquamarine-fresh',
        name: 'Aquamarine & Seafoam (Fresh / Crisp / Eco)',
        tagline: 'Vibrant turquoise-green shift. Fresh, modern, and energetic.',
        description:
            'Leans slightly toward jade and emerald undertones. Gives an ultra-modern, fresh aesthetic that makes UI components look lively and distinct from generic blue apps.',
        badge: 'Distinct & Fresh',
        recommendedLight: '#0d9488',
        recommendedDark: '#2dd4bf',
        swatches: [
            { name: 'Aqua 50', hex: '#f2fbf9', shade: 50 },
            { name: 'Aqua 100', hex: '#d1fae5', shade: 100 },
            { name: 'Aqua 200', hex: '#a7f3d0', shade: 200 },
            { name: 'Aqua 300', hex: '#6ee7b7', shade: 300 },
            { name: 'Aqua 400', hex: '#34d399', shade: 400, recommendedFor: 'dark' },
            { name: 'Aqua 500', hex: '#10b981', shade: 500, highlight: true, recommendedFor: 'both' },
            { name: 'Aqua 600', hex: '#059669', shade: 600, highlight: true, recommendedFor: 'light' },
            { name: 'Aqua 700', hex: '#047857', shade: 700 },
            { name: 'Aqua 800', hex: '#065f46', shade: 800 },
            { name: 'Aqua 900', hex: '#064e3b', shade: 900 },
            { name: 'Aqua 950', hex: '#022c22', shade: 950 },
        ],
    },
    {
        id: 'glacier-slate',
        name: 'Glacier & Slate Ice (Minimalist / Editorial / Non-Distracting)',
        tagline: 'Muted slate undertones. Ultra-refined and easy on the eyes for all-day use.',
        description:
            'A refined, desaturated architectural cyan with cool slate undertones. Eliminates eye fatigue while maintaining a crisp technological presence.',
        badge: 'Minimal & Sophisticated',
        recommendedLight: '#3d7789',
        recommendedDark: '#73b6c5',
        swatches: [
            { name: 'Glacier 50', hex: '#f0fdfa', shade: 50 },
            { name: 'Glacier 100', hex: '#e6f4f6', shade: 100 },
            { name: 'Glacier 200', hex: '#cde7ec', shade: 200 },
            { name: 'Glacier 300', hex: '#a5d3dd', shade: 300 },
            { name: 'Glacier 400', hex: '#73b6c5', shade: 400, recommendedFor: 'dark' },
            { name: 'Glacier 500', hex: '#4f93a7', shade: 500, highlight: true, recommendedFor: 'both' },
            { name: 'Glacier 600', hex: '#3d7789', shade: 600, highlight: true, recommendedFor: 'light' },
            { name: 'Glacier 700', hex: '#356170', shade: 700, recommendedFor: 'light' },
            { name: 'Glacier 800', hex: '#30505d', shade: 800 },
            { name: 'Glacier 900', hex: '#2b4550', shade: 900 },
            { name: 'Glacier 950', hex: '#192c34', shade: 950 },
        ],
    },
    {
        id: 'current-baseline',
        name: 'Current Project Baseline (#54bdce)',
        tagline: 'The color currently in XerService codebase for direct comparison.',
        description:
            'Why it looks bad: Saturation is only ~53%, which makes it look washed-out, dusty, and low-contrast in light mode, while failing to pop or glow in dark mode.',
        badge: 'Current Codebase Baseline',
        recommendedLight: '#54bdce',
        recommendedDark: '#54bdce',
        swatches: [
            { name: 'Current Accent', hex: '#54bdce', shade: 500, highlight: true },
            { name: 'Current Hover', hex: '#42b8ce', shade: 600 },
            { name: 'Current Muted Tint', hex: '#b7e4ec', shade: 300 },
            { name: 'Current Darker Step', hex: '#348e9c', shade: 700 },
            { name: 'Current Deep Navy', hex: '#1e525a', shade: 900 },
        ],
    },
];

export default function CyanPaletteStudio() {
    const { theme: appTheme, toggleTheme: appToggleTheme } = useApp();

    // Local theme override if desired, or syncs with AppContext
    const [pageTheme, setPageTheme] = useState<'light' | 'dark'>('dark');
    const [splitView, setSplitView] = useState(false);

    // Selected Cyan for the interactive sandbox & code generator
    const [selectedColor, setSelectedColor] = useState<string>('#06b6d4'); // Default Modern Tech Cyan 500
    const [selectedColorName, setSelectedColorName] = useState<string>('Modern Tech Cyan 500');
    const [selectedFamilyId, setSelectedFamilyId] = useState<string>('modern-tech');

    // Filter & Search
    const [searchQuery, setSearchQuery] = useState('');
    const [activeFilterTab, setActiveFilterTab] = useState<string>('all');

    // Feedback notifications
    const [copiedHex, setCopiedHex] = useState<string | null>(null);
    const [copiedCodeType, setCopiedCodeType] = useState<string | null>(null);
    const [isAppLivePreviewActive, setIsAppLivePreviewActive] = useState<boolean>(false);

    // Sync with app theme on mount
    useEffect(() => {
        if (appTheme) {
            setPageTheme(appTheme);
        }
    }, [appTheme]);

    // Handle copying to clipboard
    const copyToClipboard = (text: string, label: string) => {
        if (typeof navigator !== 'undefined' && navigator.clipboard) {
            navigator.clipboard.writeText(text);
            setCopiedHex(label);
            setTimeout(() => setCopiedHex(null), 2200);
        }
    };

    // Live Apply to whole app via CSS variables on :root
    const handleApplyToApp = () => {
        if (typeof document !== 'undefined') {
            const root = document.documentElement;
            const rgb = hexToRgb(selectedColor);
            const hover = getHoverVariant(selectedColor, pageTheme === 'dark');

            root.style.setProperty('--accent', selectedColor);
            root.style.setProperty('--accent-hover', hover);
            root.style.setProperty('--accent-muted', `rgba(${rgb.r}, ${rgb.g}, ${rgb.b}, 0.14)`);
            root.style.setProperty('--accent-border', `rgba(${rgb.r}, ${rgb.g}, ${rgb.b}, 0.35)`);
            root.style.setProperty('--shadow-accent', `0 8px 28px rgba(${rgb.r}, ${rgb.g}, ${rgb.b}, 0.28)`);

            setIsAppLivePreviewActive(true);
        }
    };

    const handleResetAppAccent = () => {
        if (typeof document !== 'undefined') {
            const root = document.documentElement;
            root.style.removeProperty('--accent');
            root.style.removeProperty('--accent-hover');
            root.style.removeProperty('--accent-muted');
            root.style.removeProperty('--accent-border');
            root.style.removeProperty('--shadow-accent');

            setIsAppLivePreviewActive(false);
        }
    };

    // Filtered color families
    const filteredFamilies = useMemo(() => {
        return COLOR_FAMILIES.filter(family => {
            if (activeFilterTab !== 'all' && family.id !== activeFilterTab) {
                return false;
            }
            if (!searchQuery.trim()) return true;

            const q = searchQuery.toLowerCase().trim();
            const familyMatch =
                family.name.toLowerCase().includes(q) ||
                family.description.toLowerCase().includes(q) ||
                family.badge.toLowerCase().includes(q);

            const swatchMatch = family.swatches.some(
                s =>
                    s.name.toLowerCase().includes(q) ||
                    s.hex.toLowerCase().includes(q) ||
                    s.shade.toString().includes(q)
            );

            return familyMatch || swatchMatch;
        });
    }, [activeFilterTab, searchQuery]);

    // Active color derived stats
    const activeColorRgb = hexToRgb(selectedColor);
    const contrastOnWhite = getContrastRatio(selectedColor, '#ffffff');
    const contrastOnDark = getContrastRatio(selectedColor, '#07070f');
    const hoverColor = getHoverVariant(selectedColor, pageTheme === 'dark');

    // Generated code snippets
    const cssSnippet = `/* ─── Add this into src/app/globals.css ─── */

/* Light Mode (Line 13) */
:root {
  --accent: ${selectedColor};
  --accent-hover: ${getHoverVariant(selectedColor, false)};
  --accent-fg: #ffffff;
  --accent-muted: rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.12);
  --accent-border: rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.32);
  --shadow-accent: 0 8px 28px rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.24);
}

/* Dark Mode (Line 39) */
:root.dark {
  --accent: ${selectedColor};
  --accent-hover: ${getHoverVariant(selectedColor, true)};
  --accent-muted: rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.18);
  --accent-border: rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.38);
  --shadow-accent: 0 8px 32px rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.36);
}`;

    const dualThemeSnippet = `/* Recommended Dual-Tone: Higher contrast in Light, Glowing in Dark */
:root {
  /* Light Mode (Crisp high-contrast cyan) */
  --accent: #0891b2; /* Cyan 600 */
  --accent-hover: #0e7490;
  --accent-fg: #ffffff;
  --accent-muted: rgba(8, 145, 178, 0.12);
  --accent-border: rgba(8, 145, 178, 0.30);
  --shadow-accent: 0 8px 28px rgba(8, 145, 178, 0.22);
}

:root.dark {
  /* Dark Mode (Luminous electric tech cyan) */
  --accent: #06b6d4; /* Cyan 500 */
  --accent-hover: #22d3ee;
  --accent-muted: rgba(6, 182, 212, 0.16);
  --accent-border: rgba(6, 182, 212, 0.36);
  --shadow-accent: 0 8px 32px rgba(6, 182, 212, 0.32);
}`;

    return (
        <div
            className={`palette-studio-page ${pageTheme === 'dark' ? 'dark-studio' : 'light-studio'}`}
            style={{
                background: pageTheme === 'dark' ? '#07070f' : '#f8fafc',
                color: pageTheme === 'dark' ? '#f0f0ff' : '#0a0a12',
                minHeight: '100vh',
                paddingBottom: '80px',
                transition: 'background-color 0.3s ease, color 0.3s ease',
            }}
        >
            {/* ─── Sticky Studio Navigation Bar ─── */}
            <header
                style={{
                    position: 'sticky',
                    top: 64,
                    zIndex: 90,
                    backdropFilter: 'blur(16px)',
                    WebkitBackdropFilter: 'blur(16px)',
                    background: pageTheme === 'dark' ? 'rgba(7, 7, 15, 0.88)' : 'rgba(255, 255, 255, 0.88)',
                    borderBottom: pageTheme === 'dark' ? '1px solid #1e1e38' : '1px solid #e2e8f0',
                    padding: '14px 0',
                }}
            >
                <div className="container" style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: '16px', flexWrap: 'wrap' }}>
                    <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
                        <div
                            style={{
                                width: '38px',
                                height: '38px',
                                borderRadius: '10px',
                                background: selectedColor,
                                display: 'flex',
                                alignItems: 'center',
                                justifyContent: 'center',
                                color: '#ffffff',
                                boxShadow: `0 4px 16px ${selectedColor}66`,
                                transition: 'background-color 0.25s ease',
                            }}
                        >
                            <Palette size={20} />
                        </div>
                        <div>
                            <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                                <h1 style={{ fontSize: '18px', fontWeight: '800', margin: 0, letterSpacing: '-0.02em' }}>
                                    Cyan Family Studio
                                </h1>
                                <span
                                    style={{
                                        fontSize: '11px',
                                        fontWeight: '700',
                                        padding: '2px 8px',
                                        borderRadius: '12px',
                                        background: `${selectedColor}22`,
                                        color: selectedColor,
                                        border: `1px solid ${selectedColor}44`,
                                    }}
                                >
                                    Selected: {selectedColor}
                                </span>
                            </div>
                            <p style={{ fontSize: '12px', margin: 0, color: pageTheme === 'dark' ? '#9898c0' : '#64748b' }}>
                                Find the perfect cyan replacement for <code style={{ color: '#54bdce', fontWeight: '700' }}>#54bdce</code> in light & dark mode
                            </p>
                        </div>
                    </div>

                    {/* Quick Action Controls */}
                    <div style={{ display: 'flex', alignItems: 'center', gap: '10px', flexWrap: 'wrap' }}>
                        {/* Split view toggle */}
                        <button
                            onClick={() => setSplitView(!splitView)}
                            className="btn"
                            style={{
                                fontSize: '13px',
                                padding: '7px 14px',
                                borderRadius: '8px',
                                display: 'flex',
                                alignItems: 'center',
                                gap: '6px',
                                border: splitView ? `1px solid ${selectedColor}` : pageTheme === 'dark' ? '1px solid #282845' : '1px solid #cbd5e1',
                                background: splitView ? `${selectedColor}18` : pageTheme === 'dark' ? '#121224' : '#ffffff',
                                color: splitView ? selectedColor : pageTheme === 'dark' ? '#f0f0ff' : '#1e293b',
                            }}
                        >
                            <Eye size={15} />
                            {splitView ? 'Exit Split View' : 'Side-by-Side Light/Dark'}
                        </button>

                        {/* Light / Dark Studio Theme Toggle */}
                        <button
                            onClick={() => {
                                const nextTheme = pageTheme === 'dark' ? 'light' : 'dark';
                                setPageTheme(nextTheme);
                                if (appToggleTheme && nextTheme !== appTheme) {
                                    appToggleTheme();
                                }
                            }}
                            className="btn"
                            style={{
                                fontSize: '13px',
                                padding: '7px 14px',
                                borderRadius: '8px',
                                display: 'flex',
                                alignItems: 'center',
                                gap: '6px',
                                border: pageTheme === 'dark' ? '1px solid #282845' : '1px solid #cbd5e1',
                                background: pageTheme === 'dark' ? '#121224' : '#ffffff',
                                color: pageTheme === 'dark' ? '#f0f0ff' : '#1e293b',
                            }}
                        >
                            {pageTheme === 'dark' ? <Sun size={15} color="#f59e0b" /> : <Moon size={15} color="#6366f1" />}
                            Mode: <strong>{pageTheme === 'dark' ? 'Dark' : 'Light'}</strong>
                        </button>

                        {/* Apply across app test button */}
                        {!isAppLivePreviewActive ? (
                            <button
                                onClick={handleApplyToApp}
                                className="btn"
                                style={{
                                    fontSize: '13px',
                                    fontWeight: '600',
                                    padding: '7px 14px',
                                    borderRadius: '8px',
                                    background: selectedColor,
                                    color: '#ffffff',
                                    display: 'flex',
                                    alignItems: 'center',
                                    gap: '6px',
                                    boxShadow: `0 3px 12px ${selectedColor}44`,
                                }}
                            >
                                <Zap size={14} />
                                Test Drive on Site
                            </button>
                        ) : (
                            <button
                                onClick={handleResetAppAccent}
                                className="btn"
                                style={{
                                    fontSize: '13px',
                                    fontWeight: '600',
                                    padding: '7px 14px',
                                    borderRadius: '8px',
                                    background: '#ef4444',
                                    color: '#ffffff',
                                    display: 'flex',
                                    alignItems: 'center',
                                    gap: '6px',
                                }}
                            >
                                <RefreshCw size={14} />
                                Reset Site Accent
                            </button>
                        )}
                    </div>
                </div>
            </header>

            {/* ─── Active Site Preview Banner ─── */}
            {isAppLivePreviewActive && (
                <div
                    style={{
                        background: `linear-gradient(90deg, ${selectedColor}dd, ${selectedColor})`,
                        color: '#ffffff',
                        padding: '10px 16px',
                        textAlign: 'center',
                        fontSize: '13px',
                        fontWeight: '600',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        gap: '12px',
                    }}
                >
                    <span>
                        ⚡ Live Preview Active! All XerService pages are currently styled with <strong>{selectedColor}</strong>.
                    </span>
                    <Link href="/" style={{ color: '#ffffff', textDecoration: 'underline', fontWeight: '700' }}>
                        Browse Home
                    </Link>
                    <Link href="/cart" style={{ color: '#ffffff', textDecoration: 'underline', fontWeight: '700' }}>
                        View Cart
                    </Link>
                    <button
                        onClick={handleResetAppAccent}
                        style={{
                            background: 'rgba(0,0,0,0.25)',
                            color: '#ffffff',
                            border: 'none',
                            padding: '3px 10px',
                            borderRadius: '6px',
                            cursor: 'pointer',
                            fontSize: '12px',
                        }}
                    >
                        Reset
                    </button>
                </div>
            )}

            <div className="container" style={{ marginTop: '28px' }}>
                {/* ─── Problem & Recommendation Summary ─── */}
                <section
                    style={{
                        background: pageTheme === 'dark' ? '#0f0f1d' : '#ffffff',
                        border: pageTheme === 'dark' ? '1px solid #1f1f3a' : '1px solid #e2e8f0',
                        borderRadius: '16px',
                        padding: '24px',
                        marginBottom: '32px',
                        boxShadow: pageTheme === 'dark' ? '0 8px 30px rgba(0,0,0,0.4)' : '0 4px 20px rgba(0,0,0,0.04)',
                    }}
                >
                    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: '20px', alignItems: 'center' }}>
                        <div>
                            <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '8px' }}>
                                <Sparkles size={18} color={selectedColor} />
                                <h2 style={{ fontSize: '18px', fontWeight: '800', margin: 0 }}>
                                    Why the Current Cyan (<code style={{ color: '#54bdce' }}>#54bdce</code>) Needs an Upgrade
                                </h2>
                            </div>
                            <p style={{ fontSize: '14px', lineHeight: '1.6', color: pageTheme === 'dark' ? '#a5a5c7' : '#475569', margin: '0 0 12px' }}>
                                The current project accent <code style={{ color: '#54bdce', fontWeight: '700' }}>#54bdce</code> has only ~53% saturation and 57% lightness. In <strong>Light Mode</strong>, it blends into the white background with low contrast (3.03:1), failing WCAG AA requirements for body text. In <strong>Dark Mode</strong>, it looks dusty and lacks the luminescence needed for modern fintech and SaaS interfaces.
                            </p>
                            <div style={{ display: 'flex', gap: '10px', flexWrap: 'wrap' }}>
                                <span style={{ fontSize: '12px', padding: '4px 10px', borderRadius: '6px', background: pageTheme === 'dark' ? '#18182e' : '#f1f5f9', color: pageTheme === 'dark' ? '#9898c0' : '#475569' }}>
                                    Current Contrast vs White: <strong>3.03:1 (Fails AA)</strong>
                                </span>
                                <span style={{ fontSize: '12px', padding: '4px 10px', borderRadius: '6px', background: pageTheme === 'dark' ? '#18182e' : '#f1f5f9', color: pageTheme === 'dark' ? '#9898c0' : '#475569' }}>
                                    Current Saturation: <strong>53% (Muted)</strong>
                                </span>
                            </div>
                        </div>

                        {/* Direct Comparison Swatch Block */}
                        <div
                            style={{
                                background: pageTheme === 'dark' ? '#080812' : '#f8fafc',
                                borderRadius: '12px',
                                padding: '16px',
                                border: pageTheme === 'dark' ? '1px solid #1b1b33' : '1px solid #e2e8f0',
                            }}
                        >
                            <div style={{ fontSize: '12px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.06em', marginBottom: '12px', color: pageTheme === 'dark' ? '#8888aa' : '#64748b' }}>
                                Quick Comparison: Current vs Selected
                            </div>
                            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px' }}>
                                {/* Current */}
                                <div
                                    style={{
                                        borderRadius: '10px',
                                        padding: '12px',
                                        background: pageTheme === 'dark' ? '#131326' : '#ffffff',
                                        border: '1px solid #54bdce44',
                                    }}
                                >
                                    <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '8px' }}>
                                        <div style={{ width: '22px', height: '22px', borderRadius: '6px', background: '#54bdce' }} />
                                        <span style={{ fontSize: '13px', fontWeight: '700' }}>Current</span>
                                    </div>
                                    <code style={{ fontSize: '12px', color: '#54bdce', fontWeight: '700', display: 'block', marginBottom: '4px' }}>
                                        #54bdce
                                    </code>
                                    <span style={{ fontSize: '11px', color: pageTheme === 'dark' ? '#8888aa' : '#64748b' }}>
                                        Dusty / Muted Cyan
                                    </span>
                                </div>

                                {/* Selected */}
                                <div
                                    style={{
                                        borderRadius: '10px',
                                        padding: '12px',
                                        background: pageTheme === 'dark' ? '#131326' : '#ffffff',
                                        border: `2px solid ${selectedColor}`,
                                        boxShadow: `0 4px 16px ${selectedColor}33`,
                                    }}
                                >
                                    <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '8px' }}>
                                        <div style={{ width: '22px', height: '22px', borderRadius: '6px', background: selectedColor }} />
                                        <span style={{ fontSize: '13px', fontWeight: '700' }}>Active Test</span>
                                    </div>
                                    <code style={{ fontSize: '12px', color: selectedColor, fontWeight: '700', display: 'block', marginBottom: '4px' }}>
                                        {selectedColor}
                                    </code>
                                    <span style={{ fontSize: '11px', color: pageTheme === 'dark' ? '#8888aa' : '#64748b' }}>
                                        {selectedColorName}
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </section>

                {/* ─── Interactive UI Playground Sandbox ─── */}
                <section style={{ marginBottom: '40px' }}>
                    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: '16px', flexWrap: 'wrap', gap: '12px' }}>
                        <div>
                            <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                                <Sliders size={20} color={selectedColor} />
                                <h2 style={{ fontSize: '20px', fontWeight: '800', margin: 0 }}>
                                    Interactive XerService UI Test-Drive
                                </h2>
                            </div>
                            <p style={{ fontSize: '13px', color: pageTheme === 'dark' ? '#9898c0' : '#64748b', margin: '4px 0 0' }}>
                                See how <strong style={{ color: selectedColor }}>{selectedColor}</strong> renders on actual XerService buttons, cards, badges, inputs, and order receipts.
                            </p>
                        </div>

                        {/* Active Color Info Pill */}
                        <div
                            style={{
                                display: 'flex',
                                alignItems: 'center',
                                gap: '12px',
                                background: pageTheme === 'dark' ? '#111124' : '#ffffff',
                                padding: '8px 16px',
                                borderRadius: '12px',
                                border: pageTheme === 'dark' ? '1px solid #232342' : '1px solid #cbd5e1',
                            }}
                        >
                            <div>
                                <span style={{ fontSize: '11px', color: pageTheme === 'dark' ? '#8888aa' : '#64748b', display: 'block' }}>Contrast AA on Light</span>
                                <strong style={{ fontSize: '13px', color: contrastOnWhite >= 4.5 ? '#10b981' : contrastOnWhite >= 3.0 ? '#f59e0b' : '#ef4444' }}>
                                    {contrastOnWhite}:1 {contrastOnWhite >= 4.5 ? '✓ AA Pass' : contrastOnWhite >= 3.0 ? '⚠ Large Text' : '✕ Low'}
                                </strong>
                            </div>
                            <div style={{ width: '1px', height: '24px', background: pageTheme === 'dark' ? '#232342' : '#e2e8f0' }} />
                            <div>
                                <span style={{ fontSize: '11px', color: pageTheme === 'dark' ? '#8888aa' : '#64748b', display: 'block' }}>Contrast on Dark</span>
                                <strong style={{ fontSize: '13px', color: contrastOnDark >= 4.5 ? '#10b981' : contrastOnDark >= 3.0 ? '#f59e0b' : '#ef4444' }}>
                                    {contrastOnDark}:1 {contrastOnDark >= 4.5 ? '✓ AA Pass' : '⚠ Border/Icon'}
                                </strong>
                            </div>
                            <button
                                onClick={() => copyToClipboard(selectedColor, selectedColor)}
                                className="btn"
                                style={{
                                    fontSize: '12px',
                                    padding: '5px 10px',
                                    borderRadius: '6px',
                                    background: `${selectedColor}22`,
                                    color: selectedColor,
                                    border: `1px solid ${selectedColor}44`,
                                    display: 'flex',
                                    alignItems: 'center',
                                    gap: '4px',
                                }}
                            >
                                {copiedHex === selectedColor ? <Check size={13} /> : <Copy size={13} />}
                                {copiedHex === selectedColor ? 'Copied' : 'Copy'}
                            </button>
                        </div>
                    </div>

                    {/* Render Sandbox: Either Single View or Split-View */}
                    <div
                        style={{
                            display: 'grid',
                            gridTemplateColumns: splitView ? '1fr 1fr' : '1fr',
                            gap: '24px',
                        }}
                    >
                        {/* Sandbox: Mode 1 (or Left side if split) */}
                        <div
                            style={{
                                background: splitView ? '#07070f' : pageTheme === 'dark' ? '#0b0b18' : '#ffffff',
                                color: splitView ? '#f0f0ff' : pageTheme === 'dark' ? '#f0f0ff' : '#0a0a12',
                                borderRadius: '16px',
                                padding: '24px',
                                border: splitView ? '1px solid #1e1e38' : pageTheme === 'dark' ? '1px solid #1e1e38' : '1px solid #e2e8f0',
                                boxShadow: '0 8px 30px rgba(0,0,0,0.06)',
                            }}
                        >
                            {splitView && (
                                <div style={{ display: 'flex', alignItems: 'center', gap: '6px', marginBottom: '16px' }}>
                                    <Moon size={16} color="#818cf8" />
                                    <span style={{ fontSize: '13px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.06em', color: '#818cf8' }}>
                                        Dark Mode View
                                    </span>
                                </div>
                            )}

                            {/* Simulated Components Grid */}
                            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))', gap: '20px' }}>
                                {/* 1. Buttons Preview */}
                                <div
                                    style={{
                                        background: splitView || pageTheme === 'dark' ? '#121224' : '#f8fafc',
                                        borderRadius: '12px',
                                        padding: '16px',
                                        border: splitView || pageTheme === 'dark' ? '1px solid #1e1e38' : '1px solid #e2e8f0',
                                    }}
                                >
                                    <span style={{ fontSize: '12px', fontWeight: '700', color: splitView || pageTheme === 'dark' ? '#8888aa' : '#64748b', display: 'block', marginBottom: '12px' }}>
                                        Button System
                                    </span>
                                    <div style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
                                        {/* Primary Button */}
                                        <button
                                            style={{
                                                background: selectedColor,
                                                color: '#ffffff',
                                                fontWeight: '600',
                                                padding: '10px 16px',
                                                borderRadius: '8px',
                                                border: 'none',
                                                boxShadow: `0 4px 14px ${selectedColor}44`,
                                                cursor: 'pointer',
                                                display: 'flex',
                                                alignItems: 'center',
                                                justifyContent: 'center',
                                                gap: '8px',
                                                transition: 'transform 0.15s ease',
                                            }}
                                            onMouseDown={e => (e.currentTarget.style.transform = 'scale(0.98)')}
                                            onMouseUp={e => (e.currentTarget.style.transform = 'scale(1)')}
                                        >
                                            <Zap size={16} />
                                            Print Instantly
                                        </button>

                                        {/* Muted Tint Button */}
                                        <button
                                            style={{
                                                background: `rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.14)`,
                                                color: selectedColor,
                                                fontWeight: '600',
                                                padding: '9px 16px',
                                                borderRadius: '8px',
                                                border: `1px solid rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.32)`,
                                                cursor: 'pointer',
                                                display: 'flex',
                                                alignItems: 'center',
                                                justifyContent: 'center',
                                                gap: '8px',
                                            }}
                                        >
                                            <FileText size={16} />
                                            Configure Settings
                                        </button>

                                        {/* Outline Button */}
                                        <button
                                            style={{
                                                background: 'transparent',
                                                color: selectedColor,
                                                fontWeight: '600',
                                                padding: '8px 16px',
                                                borderRadius: '8px',
                                                border: `1.5px solid ${selectedColor}`,
                                                cursor: 'pointer',
                                                display: 'flex',
                                                alignItems: 'center',
                                                justifyContent: 'center',
                                                gap: '8px',
                                            }}
                                        >
                                            Browse Local Shops
                                            <ArrowUpRight size={14} />
                                        </button>
                                    </div>
                                </div>

                                {/* 2. Order Card Preview */}
                                <div
                                    style={{
                                        background: splitView || pageTheme === 'dark' ? '#121224' : '#f8fafc',
                                        borderRadius: '12px',
                                        padding: '16px',
                                        border: splitView || pageTheme === 'dark' ? '1px solid #1e1e38' : '1px solid #e2e8f0',
                                    }}
                                >
                                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '12px' }}>
                                        <span style={{ fontSize: '12px', fontWeight: '700', color: splitView || pageTheme === 'dark' ? '#8888aa' : '#64748b' }}>
                                            Order Item Mockup
                                        </span>
                                        <span
                                            style={{
                                                fontSize: '11px',
                                                fontWeight: '700',
                                                padding: '2px 8px',
                                                borderRadius: '6px',
                                                background: `rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.16)`,
                                                color: selectedColor,
                                                border: `1px solid rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.35)`,
                                            }}
                                        >
                                            Ready for Print
                                        </span>
                                    </div>

                                    <div style={{ display: 'flex', gap: '12px', alignItems: 'center', marginBottom: '12px' }}>
                                        <div
                                            style={{
                                                width: '42px',
                                                height: '42px',
                                                borderRadius: '8px',
                                                background: `rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.16)`,
                                                color: selectedColor,
                                                display: 'flex',
                                                alignItems: 'center',
                                                justifyContent: 'center',
                                                flexShrink: 0,
                                            }}
                                        >
                                            <FileText size={22} />
                                        </div>
                                        <div>
                                            <div style={{ fontSize: '13px', fontWeight: '700' }}>Project_Thesis_Final.pdf</div>
                                            <div style={{ fontSize: '12px', color: splitView || pageTheme === 'dark' ? '#8888aa' : '#64748b' }}>
                                                14 Pages • Color • Double Sided
                                            </div>
                                        </div>
                                    </div>

                                    {/* Stepper / Progress Bar */}
                                    <div style={{ marginBottom: '12px' }}>
                                        <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: '11px', marginBottom: '4px', color: splitView || pageTheme === 'dark' ? '#8888aa' : '#64748b' }}>
                                            <span>Print Progress</span>
                                            <strong style={{ color: selectedColor }}>75%</strong>
                                        </div>
                                        <div style={{ height: '6px', width: '100%', borderRadius: '4px', background: splitView || pageTheme === 'dark' ? '#1e1e36' : '#e2e8f0', overflow: 'hidden' }}>
                                            <div style={{ height: '100%', width: '75%', background: selectedColor, borderRadius: '4px' }} />
                                        </div>
                                    </div>

                                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderTop: splitView || pageTheme === 'dark' ? '1px solid #1e1e38' : '1px solid #e2e8f0', paddingTop: '10px' }}>
                                        <span style={{ fontSize: '12px', color: splitView || pageTheme === 'dark' ? '#8888aa' : '#64748b' }}>Total Quote</span>
                                        <strong style={{ fontSize: '15px', color: selectedColor }}>₹42.00</strong>
                                    </div>
                                </div>

                                {/* 3. Badges, Inputs & XerCoins */}
                                <div
                                    style={{
                                        background: splitView || pageTheme === 'dark' ? '#121224' : '#f8fafc',
                                        borderRadius: '12px',
                                        padding: '16px',
                                        border: splitView || pageTheme === 'dark' ? '1px solid #1e1e38' : '1px solid #e2e8f0',
                                    }}
                                >
                                    <span style={{ fontSize: '12px', fontWeight: '700', color: splitView || pageTheme === 'dark' ? '#8888aa' : '#64748b', display: 'block', marginBottom: '12px' }}>
                                        Inputs & Micro-Components
                                    </span>

                                    {/* Input with Cyan Focus Ring */}
                                    <div style={{ marginBottom: '12px' }}>
                                        <label style={{ fontSize: '11px', fontWeight: '600', color: splitView || pageTheme === 'dark' ? '#8888aa' : '#64748b', display: 'block', marginBottom: '4px' }}>
                                            Active Focus Ring
                                        </label>
                                        <input
                                            type="text"
                                            readOnly
                                            value="Anna Nagar Print Hub"
                                            style={{
                                                width: '100%',
                                                padding: '8px 12px',
                                                fontSize: '13px',
                                                borderRadius: '8px',
                                                border: `1.5px solid ${selectedColor}`,
                                                boxShadow: `0 0 0 3px rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.22)`,
                                                background: splitView || pageTheme === 'dark' ? '#0b0b18' : '#ffffff',
                                                color: splitView || pageTheme === 'dark' ? '#f0f0ff' : '#0a0a12',
                                                outline: 'none',
                                            }}
                                        />
                                    </div>

                                    {/* Badge Collection */}
                                    <div style={{ display: 'flex', gap: '8px', flexWrap: 'wrap', marginBottom: '12px' }}>
                                        <span
                                            style={{
                                                fontSize: '11px',
                                                fontWeight: '700',
                                                padding: '3px 8px',
                                                borderRadius: '12px',
                                                background: `rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.16)`,
                                                color: selectedColor,
                                                border: `1px solid rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.35)`,
                                                display: 'flex',
                                                alignItems: 'center',
                                                gap: '4px',
                                            }}
                                        >
                                            <Zap size={11} /> 150 XerCoins
                                        </span>
                                        <span
                                            style={{
                                                fontSize: '11px',
                                                fontWeight: '700',
                                                padding: '3px 8px',
                                                borderRadius: '12px',
                                                background: selectedColor,
                                                color: '#ffffff',
                                            }}
                                        >
                                            Instant Pickup
                                        </span>
                                    </div>

                                    {/* Active Tab indicator */}
                                    <div style={{ display: 'flex', borderBottom: splitView || pageTheme === 'dark' ? '1px solid #1e1e38' : '1px solid #e2e8f0', gap: '16px' }}>
                                        <div
                                            style={{
                                                fontSize: '12px',
                                                fontWeight: '700',
                                                color: selectedColor,
                                                borderBottom: `2px solid ${selectedColor}`,
                                                paddingBottom: '6px',
                                            }}
                                        >
                                            All Shops (42)
                                        </div>
                                        <div style={{ fontSize: '12px', color: splitView || pageTheme === 'dark' ? '#8888aa' : '#64748b', paddingBottom: '6px' }}>
                                            Favorites
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        {/* Split-View Mode: Light Mode on the Right */}
                        {splitView && (
                            <div
                                style={{
                                    background: '#ffffff',
                                    color: '#0a0a12',
                                    borderRadius: '16px',
                                    padding: '24px',
                                    border: '1px solid #e2e8f0',
                                    boxShadow: '0 8px 30px rgba(0,0,0,0.06)',
                                }}
                            >
                                <div style={{ display: 'flex', alignItems: 'center', gap: '6px', marginBottom: '16px' }}>
                                    <Sun size={16} color="#f59e0b" />
                                    <span style={{ fontSize: '13px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.06em', color: '#f59e0b' }}>
                                        Light Mode View
                                    </span>
                                </div>

                                <div style={{ display: 'grid', gridTemplateColumns: '1fr', gap: '16px' }}>
                                    {/* Primary Button on light */}
                                    <button
                                        style={{
                                            background: selectedColor,
                                            color: '#ffffff',
                                            fontWeight: '600',
                                            padding: '10px 16px',
                                            borderRadius: '8px',
                                            border: 'none',
                                            boxShadow: `0 4px 14px ${selectedColor}44`,
                                            cursor: 'pointer',
                                            display: 'flex',
                                            alignItems: 'center',
                                            justifyContent: 'center',
                                            gap: '8px',
                                        }}
                                    >
                                        <Zap size={16} />
                                        Print Instantly
                                    </button>

                                    {/* Muted Button on light */}
                                    <button
                                        style={{
                                            background: `rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.12)`,
                                            color: selectedColor,
                                            fontWeight: '600',
                                            padding: '9px 16px',
                                            borderRadius: '8px',
                                            border: `1px solid rgba(${activeColorRgb.r}, ${activeColorRgb.g}, ${activeColorRgb.b}, 0.28)`,
                                            display: 'flex',
                                            alignItems: 'center',
                                            justifyContent: 'center',
                                            gap: '8px',
                                        }}
                                    >
                                        <FileText size={16} />
                                        Configure Settings
                                    </button>

                                    {/* Card on light */}
                                    <div
                                        style={{
                                            background: '#f8fafc',
                                            borderRadius: '12px',
                                            padding: '14px',
                                            border: '1px solid #e2e8f0',
                                        }}
                                    >
                                        <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '8px' }}>
                                            <span style={{ fontSize: '12px', fontWeight: '700', color: '#64748b' }}>Light Mode Card</span>
                                            <span style={{ fontSize: '11px', fontWeight: '700', color: selectedColor }}>₹42.00</span>
                                        </div>
                                        <div style={{ height: '6px', width: '100%', borderRadius: '4px', background: '#e2e8f0', overflow: 'hidden' }}>
                                            <div style={{ height: '100%', width: '75%', background: selectedColor }} />
                                        </div>
                                    </div>
                                </div>
                            </div>
                        )}
                    </div>
                </section>

                {/* ─── Search & Category Filters ─── */}
                <section style={{ marginBottom: '24px' }}>
                    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: '16px', flexWrap: 'wrap', marginBottom: '16px' }}>
                        {/* Search Input */}
                        <div style={{ position: 'relative', flex: '1 1 300px', maxWidth: '460px' }}>
                            <Search
                                size={17}
                                style={{
                                    position: 'absolute',
                                    left: '12px',
                                    top: '50%',
                                    transform: 'translateY(-50%)',
                                    color: pageTheme === 'dark' ? '#78789a' : '#94a3b8',
                                }}
                            />
                            <input
                                type="text"
                                placeholder="Search by hex (e.g. #06b6d4), name, or shade (500)..."
                                value={searchQuery}
                                onChange={e => setSearchQuery(e.target.value)}
                                style={{
                                    width: '100%',
                                    padding: '10px 14px 10px 38px',
                                    fontSize: '13px',
                                    borderRadius: '10px',
                                    border: pageTheme === 'dark' ? '1px solid #232342' : '1px solid #cbd5e1',
                                    background: pageTheme === 'dark' ? '#0f0f1f' : '#ffffff',
                                    color: pageTheme === 'dark' ? '#f0f0ff' : '#0a0a12',
                                    outline: 'none',
                                }}
                            />
                            {searchQuery && (
                                <button
                                    onClick={() => setSearchQuery('')}
                                    style={{
                                        position: 'absolute',
                                        right: '10px',
                                        top: '50%',
                                        transform: 'translateY(-50%)',
                                        background: 'none',
                                        border: 'none',
                                        color: pageTheme === 'dark' ? '#8888aa' : '#64748b',
                                        fontSize: '12px',
                                        cursor: 'pointer',
                                    }}
                                >
                                    ✕ Clear
                                </button>
                            )}
                        </div>

                        {/* Category Filter Pills */}
                        <div style={{ display: 'flex', gap: '8px', flexWrap: 'wrap' }}>
                            {[
                                { id: 'all', label: 'All Cyan Families' },
                                { id: 'modern-tech', label: 'Modern Tech' },
                                { id: 'electric-cyber', label: 'Electric Cyber' },
                                { id: 'sky-azure', label: 'Sky Azure' },
                                { id: 'deep-oceanic', label: 'Deep Oceanic' },
                                { id: 'aquamarine-fresh', label: 'Aquamarine' },
                                { id: 'glacier-slate', label: 'Glacier Slate' },
                                { id: 'current-baseline', label: 'Current Baseline' },
                            ].map(tab => (
                                <button
                                    key={tab.id}
                                    onClick={() => setActiveFilterTab(tab.id)}
                                    className="btn"
                                    style={{
                                        fontSize: '12px',
                                        fontWeight: activeFilterTab === tab.id ? '700' : '500',
                                        padding: '6px 12px',
                                        borderRadius: '8px',
                                        background:
                                            activeFilterTab === tab.id
                                                ? `${selectedColor}22`
                                                : pageTheme === 'dark'
                                                ? '#121224'
                                                : '#ffffff',
                                        color:
                                            activeFilterTab === tab.id
                                                ? selectedColor
                                                : pageTheme === 'dark'
                                                ? '#9898c0'
                                                : '#64748b',
                                        border:
                                            activeFilterTab === tab.id
                                                ? `1px solid ${selectedColor}66`
                                                : pageTheme === 'dark'
                                                ? '1px solid #1f1f3a'
                                                : '1px solid #e2e8f0',
                                    }}
                                >
                                    {tab.label}
                                </button>
                            ))}
                        </div>
                    </div>
                </section>

                {/* ─── Curated Cyan Families & Swatch Cards ─── */}
                <section style={{ marginBottom: '48px' }}>
                    <div style={{ display: 'flex', flexDirection: 'column', gap: '36px' }}>
                        {filteredFamilies.map(family => (
                            <div
                                key={family.id}
                                style={{
                                    background: pageTheme === 'dark' ? '#0d0d1a' : '#ffffff',
                                    border: pageTheme === 'dark' ? '1px solid #1c1c36' : '1px solid #e2e8f0',
                                    borderRadius: '16px',
                                    padding: '24px',
                                    boxShadow: pageTheme === 'dark' ? '0 4px 24px rgba(0,0,0,0.3)' : '0 2px 12px rgba(0,0,0,0.03)',
                                }}
                            >
                                {/* Family Header */}
                                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', flexWrap: 'wrap', gap: '12px', marginBottom: '16px' }}>
                                    <div>
                                        <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '4px' }}>
                                            <h3 style={{ fontSize: '18px', fontWeight: '800', margin: 0 }}>
                                                {family.name}
                                            </h3>
                                            <span
                                                style={{
                                                    fontSize: '11px',
                                                    fontWeight: '700',
                                                    padding: '2px 8px',
                                                    borderRadius: '6px',
                                                    background: `${family.recommendedDark}22`,
                                                    color: family.recommendedDark,
                                                    border: `1px solid ${family.recommendedDark}44`,
                                                }}
                                            >
                                                {family.badge}
                                            </span>
                                        </div>
                                        <p style={{ fontSize: '13px', color: pageTheme === 'dark' ? '#9898c0' : '#64748b', margin: '0 0 6px' }}>
                                            {family.tagline}
                                        </p>
                                        <p style={{ fontSize: '12px', color: pageTheme === 'dark' ? '#78789a' : '#94a3b8', margin: 0, maxWidth: '800px' }}>
                                            {family.description}
                                        </p>
                                    </div>

                                    {/* Recommended Pair shortcut buttons */}
                                    <div style={{ display: 'flex', gap: '8px' }}>
                                        <button
                                            onClick={() => {
                                                setSelectedColor(family.recommendedLight);
                                                setSelectedColorName(`${family.name} (Light Mode pick)`);
                                                setSelectedFamilyId(family.id);
                                            }}
                                            className="btn"
                                            style={{
                                                fontSize: '11px',
                                                padding: '4px 8px',
                                                borderRadius: '6px',
                                                background: pageTheme === 'dark' ? '#16162c' : '#f1f5f9',
                                                color: family.recommendedLight,
                                                border: `1px solid ${family.recommendedLight}55`,
                                                display: 'flex',
                                                alignItems: 'center',
                                                gap: '4px',
                                            }}
                                        >
                                            <Sun size={12} /> Light: {family.recommendedLight}
                                        </button>
                                        <button
                                            onClick={() => {
                                                setSelectedColor(family.recommendedDark);
                                                setSelectedColorName(`${family.name} (Dark Mode pick)`);
                                                setSelectedFamilyId(family.id);
                                            }}
                                            className="btn"
                                            style={{
                                                fontSize: '11px',
                                                padding: '4px 8px',
                                                borderRadius: '6px',
                                                background: pageTheme === 'dark' ? '#16162c' : '#f1f5f9',
                                                color: family.recommendedDark,
                                                border: `1px solid ${family.recommendedDark}55`,
                                                display: 'flex',
                                                alignItems: 'center',
                                                gap: '4px',
                                            }}
                                        >
                                            <Moon size={12} /> Dark: {family.recommendedDark}
                                        </button>
                                    </div>
                                </div>

                                {/* Swatches Grid */}
                                <div
                                    style={{
                                        display: 'grid',
                                        gridTemplateColumns: 'repeat(auto-fill, minmax(130px, 1fr))',
                                        gap: '12px',
                                    }}
                                >
                                    {family.swatches.map(swatch => {
                                        const isSelected = selectedColor.toLowerCase() === swatch.hex.toLowerCase();
                                        const swatchRgb = hexToRgb(swatch.hex);
                                        const swatchHsl = hexToHsl(swatch.hex);
                                        const crWhite = getContrastRatio(swatch.hex, '#ffffff');
                                        const crDark = getContrastRatio(swatch.hex, '#07070f');

                                        return (
                                            <div
                                                key={swatch.hex + swatch.shade}
                                                onClick={() => {
                                                    setSelectedColor(swatch.hex);
                                                    setSelectedColorName(`${family.name} • ${swatch.name}`);
                                                    setSelectedFamilyId(family.id);
                                                }}
                                                style={{
                                                    borderRadius: '12px',
                                                    overflow: 'hidden',
                                                    background: pageTheme === 'dark' ? '#121224' : '#f8fafc',
                                                    border: isSelected
                                                        ? `2px solid ${swatch.hex}`
                                                        : pageTheme === 'dark'
                                                        ? '1px solid #1f1f3a'
                                                        : '1px solid #e2e8f0',
                                                    boxShadow: isSelected ? `0 4px 18px ${swatch.hex}55` : 'none',
                                                    cursor: 'pointer',
                                                    transition: 'transform 0.15s ease, box-shadow 0.15s ease',
                                                }}
                                                onMouseEnter={e => (e.currentTarget.style.transform = 'translateY(-3px)')}
                                                onMouseLeave={e => (e.currentTarget.style.transform = 'translateY(0)')}
                                            >
                                                {/* Color Preview Tile */}
                                                <div
                                                    style={{
                                                        height: '70px',
                                                        background: swatch.hex,
                                                        position: 'relative',
                                                        display: 'flex',
                                                        alignItems: 'flex-start',
                                                        justifyContent: 'space-between',
                                                        padding: '6px 8px',
                                                    }}
                                                >
                                                    <span
                                                        style={{
                                                            fontSize: '11px',
                                                            fontWeight: '800',
                                                            color: swatchHsl.l > 60 ? '#000000' : '#ffffff',
                                                            background: 'rgba(0,0,0,0.2)',
                                                            padding: '1px 5px',
                                                            borderRadius: '4px',
                                                        }}
                                                    >
                                                        {swatch.shade}
                                                    </span>

                                                    {swatch.highlight && (
                                                        <span
                                                            style={{
                                                                fontSize: '10px',
                                                                fontWeight: '700',
                                                                color: '#ffffff',
                                                                background: 'rgba(0,0,0,0.4)',
                                                                padding: '1px 5px',
                                                                borderRadius: '4px',
                                                            }}
                                                        >
                                                            Star
                                                        </span>
                                                    )}

                                                    {isSelected && (
                                                        <div
                                                            style={{
                                                                position: 'absolute',
                                                                bottom: '6px',
                                                                right: '6px',
                                                                width: '18px',
                                                                height: '18px',
                                                                borderRadius: '50%',
                                                                background: '#ffffff',
                                                                color: swatch.hex,
                                                                display: 'flex',
                                                                alignItems: 'center',
                                                                justifyContent: 'center',
                                                                boxShadow: '0 2px 6px rgba(0,0,0,0.3)',
                                                            }}
                                                        >
                                                            <Check size={12} strokeWidth={3} />
                                                        </div>
                                                    )}
                                                </div>

                                                {/* Details & Copy Actions */}
                                                <div style={{ padding: '8px 10px' }}>
                                                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '2px' }}>
                                                        <code style={{ fontSize: '12px', fontWeight: '700', color: swatch.hex }}>
                                                            {swatch.hex}
                                                        </code>
                                                        <button
                                                            onClick={e => {
                                                                e.stopPropagation();
                                                                copyToClipboard(swatch.hex, swatch.hex);
                                                            }}
                                                            style={{
                                                                background: 'none',
                                                                border: 'none',
                                                                cursor: 'pointer',
                                                                color: pageTheme === 'dark' ? '#8888aa' : '#64748b',
                                                                padding: '2px',
                                                            }}
                                                            title="Copy HEX"
                                                        >
                                                            {copiedHex === swatch.hex ? <Check size={13} color="#10b981" /> : <Copy size={13} />}
                                                        </button>
                                                    </div>

                                                    <div style={{ fontSize: '10px', color: pageTheme === 'dark' ? '#78789a' : '#94a3b8', marginBottom: '4px' }}>
                                                        rgb({swatchRgb.r}, {swatchRgb.g}, {swatchRgb.b})
                                                    </div>

                                                    {/* Contrast ratio pills */}
                                                    <div style={{ display: 'flex', gap: '4px', marginTop: '6px' }}>
                                                        <span
                                                            title={`Contrast against white: ${crWhite}:1`}
                                                            style={{
                                                                fontSize: '9px',
                                                                fontWeight: '700',
                                                                padding: '1px 4px',
                                                                borderRadius: '3px',
                                                                background: crWhite >= 4.5 ? '#10b98122' : '#64748b22',
                                                                color: crWhite >= 4.5 ? '#10b981' : pageTheme === 'dark' ? '#9898c0' : '#64748b',
                                                            }}
                                                        >
                                                            W: {crWhite}
                                                        </span>
                                                        <span
                                                            title={`Contrast against dark #07070f: ${crDark}:1`}
                                                            style={{
                                                                fontSize: '9px',
                                                                fontWeight: '700',
                                                                padding: '1px 4px',
                                                                borderRadius: '3px',
                                                                background: crDark >= 4.5 ? '#10b98122' : '#64748b22',
                                                                color: crDark >= 4.5 ? '#10b981' : pageTheme === 'dark' ? '#9898c0' : '#64748b',
                                                            }}
                                                        >
                                                            D: {crDark}
                                                        </span>
                                                    </div>
                                                </div>
                                            </div>
                                        );
                                    })}
                                </div>
                            </div>
                        ))}
                    </div>
                </section>

                {/* ─── Instant Code Generator Section ─── */}
                <section
                    style={{
                        background: pageTheme === 'dark' ? '#0e0e1c' : '#ffffff',
                        border: pageTheme === 'dark' ? '1px solid #1f1f3a' : '1px solid #e2e8f0',
                        borderRadius: '16px',
                        padding: '24px',
                        boxShadow: '0 8px 32px rgba(0,0,0,0.06)',
                    }}
                >
                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: '12px', marginBottom: '16px' }}>
                        <div>
                            <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                                <Code size={20} color={selectedColor} />
                                <h2 style={{ fontSize: '18px', fontWeight: '800', margin: 0 }}>
                                    Ready-to-Paste Code for Finding & Replacing
                                </h2>
                            </div>
                            <p style={{ fontSize: '13px', color: pageTheme === 'dark' ? '#9898c0' : '#64748b', margin: '4px 0 0' }}>
                                Generated CSS variables calculated for your chosen cyan: <strong style={{ color: selectedColor }}>{selectedColor}</strong> ({selectedColorName}).
                            </p>
                        </div>

                        {/* Copy Code Buttons */}
                        <div style={{ display: 'flex', gap: '8px' }}>
                            <button
                                onClick={() => {
                                    copyToClipboard(cssSnippet, 'css');
                                    setCopiedCodeType('css');
                                    setTimeout(() => setCopiedCodeType(null), 2000);
                                }}
                                className="btn"
                                style={{
                                    fontSize: '12px',
                                    fontWeight: '600',
                                    padding: '7px 12px',
                                    borderRadius: '8px',
                                    background: selectedColor,
                                    color: '#ffffff',
                                    display: 'flex',
                                    alignItems: 'center',
                                    gap: '6px',
                                }}
                            >
                                {copiedCodeType === 'css' ? <Check size={14} /> : <Copy size={14} />}
                                {copiedCodeType === 'css' ? 'Copied CSS!' : 'Copy CSS Variables'}
                            </button>

                            <button
                                onClick={() => {
                                    copyToClipboard(dualThemeSnippet, 'dual');
                                    setCopiedCodeType('dual');
                                    setTimeout(() => setCopiedCodeType(null), 2000);
                                }}
                                className="btn"
                                style={{
                                    fontSize: '12px',
                                    fontWeight: '600',
                                    padding: '7px 12px',
                                    borderRadius: '8px',
                                    background: pageTheme === 'dark' ? '#18182e' : '#f1f5f9',
                                    color: pageTheme === 'dark' ? '#f0f0ff' : '#0a0a12',
                                    border: pageTheme === 'dark' ? '1px solid #282845' : '1px solid #cbd5e1',
                                    display: 'flex',
                                    alignItems: 'center',
                                    gap: '6px',
                                }}
                            >
                                {copiedCodeType === 'dual' ? <Check size={14} /> : <Copy size={14} />}
                                Copy Dual-Pair (#0891b2 / #06b6d4)
                            </button>
                        </div>
                    </div>

                    {/* Code Display Area */}
                    <div
                        style={{
                            background: pageTheme === 'dark' ? '#07070f' : '#0f172a',
                            color: '#e2e8f0',
                            borderRadius: '12px',
                            padding: '16px 20px',
                            fontFamily: 'monospace',
                            fontSize: '13px',
                            lineHeight: '1.6',
                            overflowX: 'auto',
                            border: pageTheme === 'dark' ? '1px solid #1a1a33' : '1px solid #1e293b',
                            position: 'relative',
                        }}
                    >
                        <pre style={{ margin: 0 }}>{cssSnippet}</pre>
                    </div>

                    {/* Instructions on where to paste */}
                    <div
                        style={{
                            marginTop: '16px',
                            padding: '12px 16px',
                            borderRadius: '10px',
                            background: pageTheme === 'dark' ? '#121226' : '#f8fafc',
                            border: pageTheme === 'dark' ? '1px solid #1e1e38' : '1px solid #e2e8f0',
                            display: 'flex',
                            alignItems: 'center',
                            gap: '10px',
                            fontSize: '12px',
                            color: pageTheme === 'dark' ? '#9898c0' : '#475569',
                        }}
                    >
                        <ShieldCheck size={16} color={selectedColor} style={{ flexShrink: 0 }} />
                        <span>
                            To apply permanently: Open <code style={{ color: selectedColor, fontWeight: '700' }}>src/app/globals.css</code> and replace lines <strong>13–17</strong> (for Light Mode) and lines <strong>39–43</strong> (for Dark Mode) with the code above.
                        </span>
                    </div>
                </section>
            </div>
        </div>
    );
}
