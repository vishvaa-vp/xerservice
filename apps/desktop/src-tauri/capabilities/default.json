{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default restrictive permissions for XerService desktop runtime",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
