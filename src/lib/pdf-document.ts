/**
 * PDF Document Constraints & Utilities
 * Re-exported from @packages/shared for backward compatibility.
 */

export {
    MAX_DOCUMENT_BYTES,
    MAX_DOCUMENT_PAGES,
    detectPageSize,
} from '@packages/shared';
